{
  "authenticationMethod": "route",
  "sessionTimeout": 30,
  "routes": [
    {
      "source": "^/(?!user-api)(.*)$",
      "target": "$1",
      "destination": "ne-hub-static",
      "authenticationType": "xsuaa"
    }
  ]
}
